# Force Unix line endings for shell scripts — critical for Linux containers.
# Windows Git converts LF→CRLF by default, which breaks bash scripts.
*.sh text eol=lf
Dockerfile text eol=lf
start.sh text eol=lf